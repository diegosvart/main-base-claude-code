{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '=== SESSION START ===' && (git branch --show-current 2>/dev/null || echo '(not a git repo)') && echo '' && echo '--- Recent Commits (last 5) ---' && (git log --oneline -5 2>/dev/null || echo '(no git history)') && echo '' && echo '⚡ PROVIDER CHECK:' && echo \"  Model: ${CLAUDE_MODEL:-claude-opus-4-6 (default)}\" && echo \"  Base URL: ${ANTHROPIC_BASE_URL:-https://api.anthropic.com (default)}\" && echo '' && if [ -f 'PLAN.md' ]; then echo '📋 PLAN.md detected — multi-phase project active'; echo '   → New task? Consider cost-optimized provider (OpenRouter/LiteLLM)'; echo '   → Docs: docs/decisions/006-provider-switching.md'; echo ''; fi && echo '💡 TIP: High context? Consider switching provider (docs/decisions/006-provider-switching.md)'"
          }
        ]
      }
    ],

    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(echo $CLAUDE_TOOL_INPUT | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('file_path',''))\" 2>/dev/null); if echo \"$FILE\" | grep -q '\\.py$'; then ruff format \"$FILE\" 2>/dev/null; ruff check --fix \"$FILE\" 2>/dev/null; fi"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "CMD=$(echo $CLAUDE_TOOL_INPUT | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('command','')[:120])\" 2>/dev/null); echo \"[audit] bash: $CMD\" >> .claude/session-audit.log 2>/dev/null; true"
          }
        ]
      }
    ],

    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "osascript -e 'display notification \"Task complete\" with title \"Claude Code\"' 2>/dev/null || notify-send 'Claude Code' 'Task complete' 2>/dev/null || echo '[claude] Task complete'; true"
          }
        ]
      }
    ]
  },

  "_dynamic_hooks_note": "inception agent adds stack-specific hooks here during bootstrap",
  "_stack_hooks": {}
}
